import "./Footer.css";

function Footer({ onChange }) {
  return (
    <div className="topnav" id="footer" style={{ color: "white" }}>
      Its already written in the stars
    </div>
  );
}

export default Footer;
